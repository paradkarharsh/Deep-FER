/**
 * Navbar — no longer used in the Apple HIG layout.
 * The sidebar/tab-bar handles navigation; page titles are rendered inline
 * by PageHeader within each page's scroll content.
 */

export function Navbar() {
  return null;
}
